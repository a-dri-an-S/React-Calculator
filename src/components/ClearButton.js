import { Delete } from "@styled-icons/typicons/Delete";

const ClearButton = () => {
    return ( 
        <button type="button" >
            <Delete size="60"/>
        </button>
    );
}

export default ClearButton;