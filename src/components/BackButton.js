import { Backspace } from "@styled-icons/bootstrap/Backspace";

const BackButton = () => {
    return (
        <button type="button" >
            <Backspace size="48"/>
        </button>
    );
}

export default BackButton;